model_name: microsoft/deberta-v3-base
proj_dim: 256
tau: 0.07
batch_size: 128
epochs: 15
lr: 2.0e-5
weight_decay: 0.01
warmup_ratio: 0.1
max_seq_length: 128
grad_clip: 1.0
patience: 5
seed: 42
triplets_path: data/processed/contrastive_triplets.jsonl
val_ratio: 0.1
ckpt_dir: checkpoints/embedding_2014_s1
log_path: logs/embedding_2014_s1.jsonl
grad_accum_steps: 1
gradient_checkpointing: true
